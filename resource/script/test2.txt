
var printFields = func(t)
{
  var itor = func(k,v)
  {
    ::print("key: "+k+", value: "+v);
  };
  t.iterate(itor);
};


var v = v2.new();
printFields(v2);
v = null;

/*
dofile("vec2.mn");
dofile("vec3.mn");

this.insert("printFields", printFields);

printFields(this);

var foo = fiber.new(closure.compileFile("test3.txt"));
var pos = vec2.new(0,0);

//! game loop
while (true)
{
  foo.next();
  var ch = 0;
  while ((ch = ::getch()) == -1.0) continue;
  if (ch == ' ') break;
  else if (ch == 75) pos.x -= 1;
  else if (ch == 77) pos.x += 1;
  else if (ch == 80) pos.y += 1;
  else if (ch == 72) pos.y -= 1;
  ::printxy(pos.x, pos.y, "0");
}

*/