
::TestComponent =
{ "->" : this,

new : func(ownerGO)
{
    var inst =
    {
        gameObject : ownerGO
    };

    setMeta(inst, this);
    return inst;
},

awake : func()
{
print("awaken");
},

start : func()
{
print("start");
},

update : func()
{
print("before wait");
yield WaitForSeconds.new(1);
print("middle wait");
yield WaitForSeconds.new(1);
print("after wait");
yield WaitForSeconds.new(1);
}
};
